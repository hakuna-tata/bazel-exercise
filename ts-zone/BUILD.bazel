load("@npm//@bazel/typescript:index.bzl", "ts_project")

ts_project(
  name = "ts_exec",
  tsconfig = "tsconfig.json",
  srcs = glob(["src/**", "tsconfig.json"]),
  deps = ["@npm//tsconfig-paths", "@npm//@types/node"],
  root_dir = '.',
  out_dir = 'dist',
  source_map = True,
)